import { SwipperDirective } from './swipper.directive';

describe('SwipperDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipperDirective();
    expect(directive).toBeTruthy();
  });
});
